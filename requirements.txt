# Core framework
fastapi>=0.110
uvicorn[standard]>=0.30

# ML and data processing
tensorflow==2.18.0
xgboost
numpy>=1.23,<3
pillow==11.1.0
joblib==1.4.2
shap==0.46.0

# Utilities
python-dotenv
httpx
python-multipart
requests==2.32.3
boto3

# Configuration management
pydantic>=2.9
pydantic-settings>=2.0

# Development only (not needed for API server)
# streamlit==1.41.1  — used for local client only; install separately if needed
